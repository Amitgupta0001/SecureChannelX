# Frontend Environment Variables
# Copy this file to .env in the frontend directory

# ============================================================
# AZURE APPLICATION INSIGHTS (OPTIONAL)
# ============================================================
# Leave empty for local development (will use console logging)
# Only configure when deploying to production

# Get this from: Azure Portal → Application Insights → Overview → Connection String
VITE_APPLICATIONINSIGHTS_CONNECTION_STRING=

# Example (replace with your actual connection string):
# VITE_APPLICATIONINSIGHTS_CONNECTION_STRING=InstrumentationKey=abc-123-def;IngestionEndpoint=https://eastus-8.in.applicationinsights.azure.com/
