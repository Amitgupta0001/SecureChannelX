# --- Core Framework ---
Flask==2.3.3
Flask-CORS==4.0.0
Flask-Mail==0.9.1
Flask-Bcrypt==1.0.1
Flask-JWT-Extended==4.6.0
Flask-SocketIO==5.3.5
Flask-Limiter==3.5.0
Flask-Talisman==1.1.0


# --- Socket.IO dependencies (server-side) ---
python-socketio==5.10.0
python-engineio==4.9.0

# For Socket.IO async mode (eventlet)
eventlet==0.33.3

# --- Security & Cryptography ---
cryptography==41.0.3
PyJWT==2.8.0
pyotp==2.8.0
qrcode==7.4.2
Pillow>=10.0.1

# --- Database ---
pymongo==4.5.0
certifi==2023.7.22

# --- Utilities ---
python-dotenv==1.0.0
blinker==1.6.3

# --- Messaging / Tasks ---
celery==5.3.4
redis==5.0.1

# --- Monitoring ---
prometheus-client==0.17.1

# --- Production Server ---
gunicorn==21.2.0

# --- Azure Cloud Services (Optional - with local fallbacks) ---
# Cost-optimized: All services have FREE tiers or local fallbacks
azure-keyvault-secrets==4.7.0      # Key Vault for secrets management
azure-identity==1.15.0              # Azure authentication
azure-storage-blob==12.19.0         # Blob Storage for file uploads
opencensus-ext-azure==1.1.13        # Application Insights integration
opencensus-ext-flask==0.8.2         # Flask telemetry middleware
